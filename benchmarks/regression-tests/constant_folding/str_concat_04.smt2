(set-logic ALL)
(set-info :status sat)
(declare-const s String)
(assert (= s (str.++ "foo" "bar" "baz" "boff")))
(assert (= "foobarbazboff" s))
(check-sat)

;; EXPECTED: (set-logic ALL)
;; EXPECTED: (set-info :status sat)
;; EXPECTED: (declare-const s String)
;; EXPECTED: (assert (= s "foobarbazboff"))
;; EXPECTED: (check-sat)