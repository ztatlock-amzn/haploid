(set-logic QF_S)
(declare-const r String)
(assert (str.in_re r (str.to_re "o")))
(declare-const p Bool)
(assert (= p (not (= r ""))))
(assert (not p))
(check-sat)

;; EXPECTED: (set-logic QF_S)
;; EXPECTED: (declare-const r String)
;; EXPECTED: (declare-const p Bool)
;; EXPECTED: (assert (= p false))
;; EXPECTED: (assert (= p (not (= r ""))))
;; EXPECTED: (assert (not p))
;; EXPECTED: (assert (str.in_re r (str.to_re "o")))
;; EXPECTED: (check-sat)